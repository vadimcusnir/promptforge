# Brand Layer — Rune Executabilă
- Logo = sigiliu, nu decor. ASCII-shape (> | { } ~ .) + sacred geometry.
- Use animated SVG in UI (glow, pulse). Static on PDF/export.
- Each passed prompt (score >=80) can mint a per-run 'rune' (SVG ID).